# Pinned Fluent icons version/tag reference.
# Keep this file machine-readable: first non-comment non-empty line is used.
#
# The value is used as-is for raw.githubusercontent.com ref resolution.
# Example tag for fluentui-system-icons: `1.1.319` (no `v` prefix).

1.1.335
