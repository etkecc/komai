    // Network
    CONNECT_SETTING_ID(NetworkPresenceStatusPolicy, presenceChanged, Value);
    CONNECT_SETTING_ID(
      NetworkTlsEnableCertificateValidation, certificateValidationEnabledChanged, Value);
    CONNECT_SETTING_ID(NetworkHttp3Enabled, http3EnabledChanged, Value);
