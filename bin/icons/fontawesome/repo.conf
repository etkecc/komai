# Pinned Font Awesome source.
# Keep source location pinned for reproducibility.
#
# This file is committed config, not scratch data.
# `sync.sh` reads it to resolve the exact upstream source.
# Version/ref pin lives in `bin/icons/fontawesome/VERSION_REF`.

FA_REPO="FortAwesome/Font-Awesome"
