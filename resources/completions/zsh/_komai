#compdef komai
# zsh completion for komai (auto-generated; do not edit)

_komai() {
    local -a _komai_value_flags=(--access --alias-localpart --before-event-id --caption --content-type --creation-content --fetch-mode --filename --format --initial-state --invite --limit --log-level --log-type --msgtype --name --power-levels --preset --profile --reason --room-version --topic --variant -L -l -p)
    local cur prev
    cur="${words[CURRENT]}"
    prev="${words[CURRENT-1]}"

    case "${prev}" in
        --access) _values "access" read_only read_write; return ;;
        --fetch-mode) _values "fetch-mode" cached_only server_fetch_if_needed; return ;;
        --format) _values "format" auto plain html; return ;;
        --msgtype) _values "msgtype" text notice; return ;;
        --preset) _values "preset" private_chat public_chat trusted_private_chat; return ;;
        --variant) _values "variant" light dark; return ;;
    esac

    local -a _komai_path=()
    local _skip=0 _i
    for ((_i=2; _i<CURRENT; _i++)); do
        local _w="${words[_i]}"
        if [[ $_skip -eq 1 ]]; then _skip=0; continue; fi
        local _is_value_flag=0 _f
        for _f in "${_komai_value_flags[@]}"; do
            if [[ "${_w}" == "${_f}" ]]; then _is_value_flag=1; break; fi
        done
        if [[ $_is_value_flag -eq 1 ]]; then _skip=1; continue; fi
        [[ "${_w}" == -* ]] && continue
        _komai_path+=("${_w}")
    done

    local _key="${_komai_path[*]}"
    case "${_key}" in
        'profiles launcher remove' | 'profiles launcher remove *') compadd -- --help -h ;;
        'profiles launcher create' | 'profiles launcher create *') compadd -- --help -h ;;
        'settings ui set-theme' | 'settings ui set-theme *') compadd -- --help -h ;;
        'settings ui theme' | 'settings ui theme *') compadd -- --help -h ;;
        'rooms invite' | 'rooms invite *') compadd -- --help --reason -h ;;
        'rooms create' | 'rooms create *') compadd -- --alias-localpart --creation-content --direct --encrypted --help --initial-state --invite --name --power-levels --preset --public --room-version --space --topic -h ;;
        'rooms kick' | 'rooms kick *') compadd -- --help --reason -h ;;
        'rooms ban' | 'rooms ban *') compadd -- --help --reason -h ;;
        'rooms unban' | 'rooms unban *') compadd -- --help --reason -h ;;
        'rooms leave' | 'rooms leave *') compadd -- --help --reason -h ;;
        'rooms send' | 'rooms send *') compadd -- --format --help --msgtype -h ;;
        'rooms send-image' | 'rooms send-image *') compadd -- --caption --filename --help -h ;;
        'settings ui' | 'settings ui *') compadd -- --help -h set-theme theme ;;
        'theme list' | 'theme list *') compadd -- --help -h ;;
        'theme tinted-search' | 'theme tinted-search *') compadd -- --help -h ;;
        'theme tinted-import' | 'theme tinted-import *') compadd -- --force --help --variant -h ;;
        'theme create-sample' | 'theme create-sample *') compadd -- --force --help -h ;;
        'user id' | 'user id *') compadd -- --help -h ;;
        'user homeserver-url' | 'user homeserver-url *') compadd -- --help -h ;;
        'user device-id' | 'user device-id *') compadd -- --help -h ;;
        'user status' | 'user status *') compadd -- --help -h ;;
        'user set-status' | 'user set-status *') compadd -- --help -h ;;
        'rooms new-direct-chat' | 'rooms new-direct-chat *') compadd -- --help -h ;;
        'app version' | 'app version *') compadd -- --help -h ;;
        'app api-version' | 'app api-version *') compadd -- --help -h ;;
        'completions bash' | 'completions bash *') compadd -- --help -h ;;
        'completions zsh' | 'completions zsh *') compadd -- --help -h ;;
        'completions fish' | 'completions fish *') compadd -- --help -h ;;
        'media fetch' | 'media fetch *') compadd -- --help -h ;;
        'media upload' | 'media upload *') compadd -- --content-type --filename --help --stdin -h ;;
        'mcp serve' | 'mcp serve *') compadd -- --access --help -h ;;
        'profiles launcher' | 'profiles launcher *') compadd -- --help -h create remove ;;
        'rooms list' | 'rooms list *') compadd -- --help -h ;;
        'rooms timeline' | 'rooms timeline *') compadd -- --before-event-id --fetch-mode --help --include-unsigned-fields --limit -h ;;
        'rooms join' | 'rooms join *') compadd -- --help -h ;;
        'settings' | 'settings *') compadd -- --help -h ui ;;
        'mcp' | 'mcp *') compadd -- --help -h serve ;;
        'profiles' | 'profiles *') compadd -- --help -h launcher ;;
        'theme' | 'theme *') compadd -- --help -h create-sample list tinted-import tinted-search ;;
        'media' | 'media *') compadd -- --help -h fetch upload ;;
        'rooms' | 'rooms *') compadd -- --help -h ban create invite join kick leave list new-direct-chat send send-image timeline unban ;;
        'user' | 'user *') compadd -- --help -h device-id homeserver-url id set-status status ;;
        'completions' | 'completions *') compadd -- --help -h bash fish zsh ;;
        'app' | 'app *') compadd -- --help -h api-version version ;;
        '') compadd -- --debug --help --log-level --log-type --profile --version -L -h -l -p -v app completions mcp media profiles rooms settings theme user ;;
    esac
}

_komai "$@"
